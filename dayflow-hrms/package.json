{
  "name": "dayflow-hrms",
  "version": "1.0.0",
  "description": "Dayflow - Human Resource Management System",
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "concurrently --kill-others \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
